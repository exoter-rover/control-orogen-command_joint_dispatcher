# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(command_joint_dispatcherTaskLib)
ADD_LIBRARY(${COMMAND_JOINT_DISPATCHER_TASKLIB_NAME} SHARED 
    ${COMMAND_JOINT_DISPATCHER_TASKLIB_SOURCES})
add_dependencies(${COMMAND_JOINT_DISPATCHER_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${COMMAND_JOINT_DISPATCHER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${COMMAND_JOINT_DISPATCHER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${COMMAND_JOINT_DISPATCHER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${COMMAND_JOINT_DISPATCHER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${COMMAND_JOINT_DISPATCHER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${COMMAND_JOINT_DISPATCHER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${COMMAND_JOINT_DISPATCHER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${COMMAND_JOINT_DISPATCHER_TASKLIB_HEADERS}
    DESTINATION include/orocos/command_joint_dispatcher)

